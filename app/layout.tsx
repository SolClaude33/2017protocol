import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata = {
  title: "The 2017 Protocol — Post-Internet AI Console",
  description: "Step into a post-apocalyptic terminal simulating the ‘Dead Internet’. Converse with $NULL — an AI remnant navigating echoing nodes, lost protocols, and cache ghosts.",
  icons: {
    icon: "/abstract-red-digital-face-glitching.jpg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
